telemetry:
  logging:
    level: debug  # debug, info, warn, error, fatal
    encoding: console

#api:
#  http:
#    ...

routers:
  language:
    - id: myrouter
      models:
        - id: openai
          openai:
            apiKey: ""
            model: "gpt-3.5-turbo"
